.errorContainer {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: var(--pad);
  font-size: 1.2em;

  img {
    user-select: none;
    opacity: .3;
    height: 100px;
    margin: var(--pad);
  }
}

.errorContainer.dark {
  img {
    filter: invert(100%);
  }
}